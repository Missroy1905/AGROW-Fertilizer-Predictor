demo video ->
![final-agroww-ezgif com-crop](https://github.com/user-attachments/assets/6bc935a2-3d7a-4c26-bfe6-575172d48a38)
🌱 AGROW-Fertilizer-Predictor: Precision Fertilizer Prediction💡 OverviewThe AGROW-Fertilizer-Predictor is a web-based system designed to provide data-driven recommendations for optimal fertilizer usage. It employs a Logistic Regression machine learning model, which is trained on soil and environmental data to predict the ideal level or type of fertilizer required for a given crop.This project promotes sustainable and efficient farming by minimizing resource waste and maximizing crop yield.✨ FeaturesIntelligent Prediction: Utilizes a pre-trained Logistic Regression model (classifier.pkl) for quick and accurate fertilizer level prediction.Web Application: Served via main.py with the user interface defined in the templates directory (specifically index.html).Data Driven: Based on the analysis performed in Fertilizer Prediction.ipynb and trained on Fertilizer Prediction.csv.Database Integration: Uses users.db for potential user management or storing prediction history.🚀 Getting StartedFollow these steps to set up and run the application locally.PrerequisitesPython: Version 3.8 or higher.Git: For cloning the repository.InstallationClone the repository:Bashgit clone https://github.com/Missroy1905/AGROW-Fertilizer-Predictor.git
cd AGROW-Fertilizer-Predictor
Create a virtual environment (Recommended):Bashpython -m venv venv
source venv/bin/activate  # On Linux/macOS
# .\venv\Scripts\activate  # On Windows
Install the required dependencies:Bashpip install -r requirements.txt
Running the ApplicationThe main application is served using main.py.Start the web server:Bashpython main.py
Access the application in your web browser at the address provided in the terminal (usually http://127.0.0.1:5000/).🔬 Model and TrainingThe machine learning core of this project is a Logistic Regression model.Model File: The final, trained model is saved as classifier.pkl.Auxiliary Data: The file fertilizer.pkl likely contains the training labels, scaler objects, or other necessary components for the model to operate correctly.Training Analysis: The comprehensive training and evaluation process can be reviewed in the Jupyter Notebook: Fertilizer Prediction.ipynb.Training Data: The model was trained using the data source: Fertilizer Prediction.csv.Note: If you modify the training data or the model parameters in the Jupyter Notebook, remember to re-save the resulting model (classifier.pkl and fertilizer.pkl) to update the application's prediction logic.📂 Project StructureThis is the file structure of the repository:File/DirectoryDescriptionmain.pyThe main application file (likely a Flask/Streamlit app) that serves the web interface and handles model prediction logic.templates/Directory containing the front-end HTML files (e.g., index.html) for the user interface.Fertilizer Prediction.csvThe raw dataset used for training the ML model.Fertilizer Prediction.ipynbJupyter Notebook detailing the data cleaning, feature engineering, training, and evaluation of the Logistic Regression model.classifier.pklThe pre-trained Logistic Regression model used for making real-time predictions.fertilizer.pklA supplementary pickle file, possibly containing necessary objects like label encoders, scalers, or the target variable list.requirements.txtLists all Python libraries and dependencies required to run the project.users.dbA SQLite database file, likely used for storing user credentials or prediction history.app.yamlConfiguration file, possibly for deployment on platforms like Google App Engine.
